<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingTop="100dp"
    tools:context=".addNotification.AddActivity">

    <EditText
        android:id="@+id/notificationTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Notification Title" />

    <EditText
        android:id="@+id/notificationDetail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Notification Detail" />

    <EditText
        android:id="@+id/promo_code"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Promo code"/>


    <RelativeLayout
        android:id="@+id/spinner_bg"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_below="@+id/text_login_help"
        android:background="@drawable/spinner_border"
        android:layout_marginTop="20dp">
        <Spinner
            android:id="@+id/image_url"
            style="@style/Widget.AppCompat.DropDownItem.Spinner"
            android:layout_width="match_parent"
            android:layout_height="70dp"
            android:entries="@array/image_url"/>
    </RelativeLayout>

    <Button
        android:id="@+id/btn_set_validity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="Set Validity" />


</LinearLayout>
